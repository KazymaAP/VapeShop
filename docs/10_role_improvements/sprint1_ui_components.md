# 🎨 UI КОМПОНЕНТЫ И СТРАНИЦЫ — SPRINT 1

**Спринт 1: Super-Admin + Admin**  
**Статус:** Компонентная архитектура  
**Директория:** `pages/admin/super/` и `pages/admin/` (расширение)

---

## 📄 НОВЫЕ СТРАНИЦЫ

### 1. `/admin/super/index.tsx` — Дашборд Super-Admin

**Назначение:** Главная страница для super-администратора

**Макет:**

```
┌─────────────────────────────────────────────┐
│  VapeShop Super-Admin Dashboard             │
├─────────────────────────────────────────────┤
│                                             │
│  [Управление админами] [Логи] [RBAC] [⚙️]  │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ АКТИВНЫЕ АДМИНИСТРАТОРЫ (5)          │  │
│  ├──────────────────────────────────────┤  │
│  │ Админ 1 | admin | Активен | [Удалить] │  │
│  │ Админ 2 | admin | Активен | [Удалить] │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ СИСТЕМА ЛОГИРОВАНИЯ                  │  │
│  ├──────────────────────────────────────┤  │
│  │ Всего логов: 12,456                  │  │
│  │ Сегодня: 234                         │  │
│  │ За неделю: 1,456                     │  │
│  │                                      │  │
│  │ [Просмотреть все логи] →             │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ ПОСЛЕДНИЕ ДЕЙСТВИЯ                   │  │
│  ├──────────────────────────────────────┤  │
│  │ 14:30 | Админ2 | Обновил товар #456 │  │
│  │ 14:15 | Админ1 | Выгрузил отчёт     │  │
│  │ 13:45 | Админ3 | Удалил категорию   │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ ГЛОБАЛЬНЫЕ НАСТРОЙКИ                 │  │
│  ├──────────────────────────────────────┤  │
│  │ Комиссия: 15% [Изменить]            │  │
│  │ Мин. заказ: 100₽ [Изменить]         │  │
│  │ Налог: 18% [Изменить]               │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

**Props:**

```typescript
interface SuperAdminDashboardProps {
  admins: Admin[];
  recentLogs: AuditLog[];
  stats: {
    totalLogs: number;
    todayLogs: number;
    weekLogs: number;
  };
}
```

**Импорты и использование:**

```typescript
import { useState, useEffect } from 'react';
import AdminLayout from '../../components/AdminLayout';
import Card from '../../components/Card';
import Modal from '../../components/Modal';
import Toast from '../../components/Toast';
import { useTelegramWebApp } from '../../lib/telegram';

export default function SuperAdminDashboard() {
  const { user } = useTelegramWebApp();
  const [admins, setAdmins] = useState([]);
  const [logs, setLogs] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchDashboardData();
  }, []);

  const fetchDashboardData = async () => {
    try {
      // Получить админов (новый API)
      // Получить логи (из audit-logs.ts)
      // Получить RBAC роли
    } catch (err) {
      console.error('Failed to fetch dashboard:', err);
    }
  };

  return (
    <AdminLayout title="Super-Admin Dashboard">
      {/* Управление админами */}
      {/* Просмотр логов */}
      {/* Управление ролями */}
      {/* Глобальные настройки */}
    </AdminLayout>
  );
}
```

---

### 2. `/admin/super/roles.tsx` — Управление ролями (RBAC)

**Назначение:** Создание, редактирование, удаление ролей и разрешений

**Макет:**

```
┌──────────────────────────────────────────────────┐
│  RBAC Manager - Управление ролями               │
├──────────────────────────────────────────────────┤
│                                                  │
│  [+ Создать роль]                               │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ РОЛЬ: super_admin                          │ │
│  ├────────────────────────────────────────────┤ │
│  │ Описание: Super Administrator - Full access │ │
│  │ Система роль: ДА (не удаляется)            │ │
│  │                                            │ │
│  │ РАЗРЕШЕНИЯ (7):                            │ │
│  │ ☑ users:read         [x]                   │ │
│  │ ☑ users:write        [x]                   │ │
│  │ ☑ products:read      [x]                   │ │
│  │ ☑ audit:read         [x]                   │ │
│  │ ... ещё 3                                  │ │
│  │                                            │ │
│  │ [Добавить разрешение ▼] [Сохранить]      │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ РОЛЬ: admin                                │ │
│  ├────────────────────────────────────────────┤ │
│  │ Описание: Administrator                    │ │
│  │ Система роль: ДА                           │ │
│  │ РАЗРЕШЕНИЯ (12): products:*, orders:*, ... │ │
│  │                                            │ │
│  │ [Редактировать] [Дублировать] [Удалить]   │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ РОЛЬ: content_manager (ПОЛЬЗОВАТЕЛЬСКАЯ)  │ │
│  ├────────────────────────────────────────────┤ │
│  │ РАЗРЕШЕНИЯ (5): banners:*, faq:*, ...     │ │
│  │                                            │ │
│  │ [Редактировать] [Дублировать] [Удалить] │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
└──────────────────────────────────────────────────┘
```

**Функционал:**

- Список всех ролей (система и пользовательские)
- Просмотр разрешений для каждой роли
- Добавление новых разрешений к роли
- Удаление разрешений
- Создание новой роли (модальное окно)
- Удаление пользовательских ролей

**Компонент Modal для создания роли:**

```typescript
<Modal title="Создать новую роль" isOpen={showCreateRole} onClose={() => setShowCreateRole(false)}>
  <form onSubmit={handleCreateRole}>
    <input placeholder="Название роли" value={roleName} onChange={e => setRoleName(e.target.value)} />
    <textarea placeholder="Описание" value={roleDescription} onChange={e => setRoleDescription(e.target.value)} />
    <MultiSelect
      options={allPermissions}
      selected={selectedPermissions}
      onChange={setSelectedPermissions}
      placeholder="Выберите разрешения"
    />
    <button type="submit">Создать</button>
  </form>
</Modal>
```

---

### 3. `/admin/logs.tsx` — Просмотр логов (расширение)

**Назначение:** Детальная таблица с фильтрацией логов

**Макет:**

```
┌─────────────────────────────────────────────────────────┐
│  AUDIT LOGS - Логирование действий                     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ФИЛЬТРЫ:                                               │
│  [Пользователь ▼] [Действие ▼] [Тип ▼] [Дата ▼]       │
│  [Применить] [Сбросить] [Экспортировать логи]         │
│                                                         │
│  ТАБЛИЦА ЛОГОВ:                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Время      │ Администратор │ Действие │ Объект  │   │
│  ├─────────────────────────────────────────────────┤   │
│  │14:30:45    │ Админ2        │ UPDATE   │Product  │   │
│  │            │               │ #456     │IQOS     │   │
│  │            │               │          │heets    │   │
│  │            │ IP: 192.168.1.1 │ Status: SUCCESS   │   │
│  │            │ Details: ↓                         │   │
│  │            │ {old_price: 500, new_price: 450}   │   │
│  │            │ [Развернуть детали]                │   │
│  ├─────────────────────────────────────────────────┤   │
│  │14:15:30    │ Админ1        │ EXPORT   │Orders   │   │
│  │            │ IP: 192.168.1.2 │ Status: SUCCESS   │   │
│  │            │ [Развернуть детали]                │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  Страница 1 из 25  [< Пред] [1 2 3 ... 25] [След >]  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 4. `/admin/products/bulk-edit.tsx` — Массовое редактирование

**Назначение:** Таблица товаров с возможностью массового обновления

**Макет:**

```
┌───────────────────────────────────────────────────┐
│  BULK EDIT PRODUCTS - Массовое редактирование    │
├───────────────────────────────────────────────────┤
│                                                   │
│  ФИЛЬТРЫ:                                         │
│  [Категория ▼] [Бренд ▼] [Статус ▼]             │
│  [Найти товары]                                   │
│                                                   │
│  ТАБЛИЦА (выбрано 0 товаров):                     │
│  ┌────┬──────────┬───────┬────────┬──────────┐   │
│  │ ☐  │ Товар    │ Цена  │ Скидка │ Статус   │   │
│  ├────┼──────────┼───────┼────────┼──────────┤   │
│  │ ☑  │ IQOS h.  │ 500₽  │ 0%     │ active   │   │
│  │ ☑  │ Lost Mary│ 250₽  │ 15%    │ active   │   │
│  │ ☐  │ Vape X   │ 180₽  │ 10%    │ new      │   │
│  └────┴──────────┴───────┴────────┴──────────┘   │
│                                                   │
│  ДЕЙСТВИЯ ДЛЯ ВЫБРАННЫХ:                          │
│  ┌─────────────────────────────────────────────┐ │
│  │ Действие с ценой:                           │ │
│  │ ○ Увеличить на % [15] %  [Применить]       │ │
│  │ ○ Уменьшить на % [10] %  [Применить]       │ │
│  │ ○ Установить цену [450] ₽ [Применить]     │ │
│  │                                             │ │
│  │ Другие:                                     │ │
│  │ Новая категория: [Выбрать ▼]               │ │
│  │ Новый бренд: [Выбрать ▼]                   │ │
│  │ Новый статус: [Выбрать ▼]                  │ │
│  │ Отметить как "hit": [☑] [☐]                │ │
│  │ Отметить как "new": [☑] [☐]                │ │
│  │                                             │ │
│  │ [Применить ко всем] [Отмена]               │ │
│  └─────────────────────────────────────────────┘ │
│                                                   │
└───────────────────────────────────────────────────┘
```

---

### 5. `/admin/dashboard.tsx` — Расширенный дашборд Admin

**Назначение:** Аналитика с графиками (используется recharts)

**Макет:**

```
┌──────────────────────────────────────────────────────┐
│  Advanced Analytics Dashboard                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ПЕРИОД: [День] [Неделя] [Месяц] [Год]             │
│  ДАТА: [От] 2024-01-01 [До] 2024-04-03 [Обновить] │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │ KPI:                                         │  │
│  │ Выручка: 150,000₽ | Заказов: 420           │  │
│  │ Средний чек: 357₽ | Новых клиентов: 85     │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │ ВЫРУЧКА ПО ДНЯМ (График)                    │  │
│  │ [Line Chart: По дням]                       │  │
│  └──────────────────────────────────────────────┘  │
│                                                      │
│  ┌────────────────────┐  ┌────────────────────┐   │
│  │ ТОП-10 ТОВАРОВ     │  │ ТОП КАТЕГОРИИ      │   │
│  │ 1. IQOS heets      │  │ 1. Pod Systems     │   │
│  │ 2. Lost Mary       │  │ 2. E-liquids       │   │
│  │ 3. Vape Brand      │  │ 3. Accessories     │   │
│  │ ... ещё 7          │  │ ... ещё 2          │   │
│  └────────────────────┘  └────────────────────┘   │
│                                                      │
│  ┌────────────────────┐  ┌────────────────────┐   │
│  │ ТОП БРЕНДЫ         │  │ ПРОДАЖИ ПО ГОРОДАМ │   │
│  │ 1. IQOS            │  │ Москва: 300 заказ  │   │
│  │ 2. Vape Brand      │  │ СПБ: 120 заказов   │   │
│  │ ... ещё 1          │  │ Казань: 50 заказов │   │
│  └────────────────────┘  └────────────────────┘   │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**Графики (recharts):**

```typescript
import { LineChart, Line, BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';

// Линейный график выручки
<ResponsiveContainer width="100%" height={300}>
  <LineChart data={revenueData}>
    <CartesianGrid strokeDasharray="3 3" />
    <XAxis dataKey="date" />
    <YAxis />
    <Tooltip />
    <Legend />
    <Line type="monotone" dataKey="revenue" stroke="#c084fc" />
    <Line type="monotone" dataKey="orders" stroke="#818cf8" />
  </LineChart>
</ResponsiveContainer>
```

---

### 6. `/admin/orders/export.tsx` — Экспорт заказов

**Назначение:** Форма для экспорта данных в Excel, CSV, PDF

**Макет:**

```
┌─────────────────────────────────────────────────┐
│  EXPORT ORDERS - Экспорт заказов               │
├─────────────────────────────────────────────────┤
│                                                 │
│  ФОРМАТ ЭКСПОРТА:                               │
│  ○ Excel (.xlsx)  ○ CSV (.csv)  ○ PDF (.pdf)  │
│                                                 │
│  ФИЛЬТРЫ:                                       │
│  Дата от: [2024-01-01] До: [2024-04-03]       │
│  Статус: [Все ▼] [Активные ▼]                │
│  Сумма от: [0] До: [999999]                    │
│                                                 │
│  ВЫБРАТЬ ПОЛЯ (для Excel/CSV):                 │
│  ☑ Номер заказа                                │
│  ☑ Клиент                                      │
│  ☑ Сумма                                       │
│  ☑ Статус                                      │
│  ☑ Способ доставки                             │
│  ☑ Адрес доставки                              │
│  ☑ Дата заказа                                 │
│  ☑ Примечания менеджера                        │
│  ... ещё 5 полей                               │
│                                                 │
│  СОРТИРОВКА:                                    │
│  По: [Дата ▼] Порядок: [По убыванию ▼]       │
│                                                 │
│  [Предпросмотр] [Экспортировать] [Отмена]    │
│                                                 │
│  Статус: [════════════] 0%                     │
│                                                 │
└─────────────────────────────────────────────────┘
```

**Использование exceljs:**

```typescript
import ExcelJS from 'exceljs';

const workbook = new ExcelJS.Workbook();
const worksheet = workbook.addWorksheet('Orders');

worksheet.columns = [
  { header: 'Order ID', key: 'id', width: 12 },
  { header: 'Customer', key: 'customer', width: 20 },
  { header: 'Amount', key: 'amount', width: 12 },
  // ...
];

orders.forEach((order) => {
  worksheet.addRow(order);
});

const buffer = await workbook.xlsx.writeBuffer();
// Скачать файл
```

---

## 🧩 ПЕРЕИСПОЛЬЗУЕМЫЕ КОМПОНЕНТЫ

### 1. Card

```typescript
<Card title="Управление админами" className="mb-4">
  {/* Контент */}
</Card>
```

### 2. Modal

```typescript
<Modal
  title="Создать акцию"
  isOpen={isOpen}
  onClose={onClose}
  onSubmit={onSubmit}
>
  {/* Форма */}
</Modal>
```

### 3. Table

```typescript
<Table
  headers={['ID', 'Название', 'Цена', 'Действия']}
  rows={products}
  onSelect={setSelectedRows}
/>
```

### 4. Toast (для уведомлений)

```typescript
const { toast } = useToast();
toast({ message: 'Товары обновлены', type: 'success' });
```

---

## 📁 ФАЙЛОВАЯ СТРУКТУРА UI

```
pages/admin/
├── super/
│   ├── index.tsx              # Дашборд super-admin ✓
│   ├── admins.tsx             # Управление админами
│   └── roles.tsx              # Управление ролями (RBAC) ✓
├── logs.tsx                   # Просмотр логов ✓
├── dashboard.tsx              # Расширенный дашборд (обновить) ✓
├── orders/
│   └── export.tsx             # Экспорт заказов ✓
├── products/
│   ├── index.tsx              # Список товаров (существует)
│   └── bulk-edit.tsx          # Массовое редактирование ✓
├── promotions/
│   ├── index.tsx              # Список акций
│   └── [id].tsx               # Редактирование акции
└── gift-certificates/
    ├── index.tsx              # Список сертификатов
    └── [id].tsx               # Детали сертификата
```

---

## 🎯 ДИЗАЙН РЕКОМЕНДАЦИИ

- **Цветовая схема:** Neon Dark Theme (как уже в проекте)
- **Основной цвет:** `#c084fc` (фиолетовый неон)
- **Фон:** `#0a0a0f` (очень тёмный)
- **Карты:** `#111115` (тёмно-серый)
- **Текст:** `#e0e0e0` (светлый серый)
- **Шрифт:** Inter (система-ui fallback)

**Tailwind классы:**

```typescript
<div className="bg-cardBg border border-border rounded-lg p-4">
  <h2 className="text-lg text-textPrimary font-semibold">Заголовок</h2>
  <p className="text-textSecondary">Описание</p>
</div>
```

---

## ✅ ЧЕКЛИСТ РЕАЛИЗАЦИИ UI

- [ ] `/admin/super/index.tsx` создана и работает
- [ ] `/admin/super/roles.tsx` (RBAC manager) готова
- [ ] `/admin/logs.tsx` отображает таблицу логов
- [ ] `/admin/dashboard.tsx` расширена с recharts графиками
- [ ] `/admin/products/bulk-edit.tsx` готова
- [ ] `/admin/orders/export.tsx` готова
- [ ] Все компоненты используют Tailwind классы Neon theme
- [ ] Все модали и формы функциональны
- [ ] Мобильный адаптив проверен

---

**Документ подготовлен:** Copilot CLI  
**Дата:** 2026-04-03  
**Следующий документ:** `sprint2_api_endpoints.md`
