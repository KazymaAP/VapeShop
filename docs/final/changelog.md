# 📝 CHANGELOG

## 2026-04-04

### ✅ Выполненные задачи

#### 🔴 Критические (Фаза 1)

**Задача 1: HMAC-валидация X-Telegram-Id** ✓
- Переписана `getTelegramIdFromRequest()` с приоритетом на верифицированную `initData`
- Функция `verifyTelegramInitData()` теперь экспортируется
- X-Telegram-Id используется только для локальной разработки (NODE_ENV !== 'production')

**Задача 2: Защита /api/orders.ts** ✓
- Импорт функции `isUserBlocked` из `lib/auth.ts`
- Добавлена проверка блокировки пользователя

**Задача 3: Защита всех admin API** ✓
- Все 29 файлов в папке admin защищены `requireAuth`

**Задача 4: Проверка блокировки пользователя при платежах** ✓
- Добавлена проверка `isUserBlocked(currentTelegramId)` перед созданием заказа

**Задача 5: SQL-инъекции в динамических запросах** ✓
- Создана утилита `lib/sqlBuilder.ts` для безопасного построения SQL запросов
- Использован белый список полей для каждой таблицы
- Исправлены 4 файла: `faq/index.ts`, `pickup-points.ts`, `pages/index.ts`, `promocodes/index.ts`
- Обнаружена и исправлена критическая SQL-инъекция в `admin/roles.ts` (строка 34)

**Задача 6: Rate limiting для критических эндпоинтов** ✓
- Rate limiting уже реализован в `lib/rateLimit.ts`
- Применён на: `/api/orders.ts`, `/api/admin/roles.ts`, `/api/bot.ts`
- Исправлена SQL-инъекция в `/api/admin/roles.ts` с параметризованными запросами

**Задача 7: Верификация webhook Telegram Bot** ✓
- Добавлена проверка заголовка `x-telegram-bot-api-secret-token` в `/api/bot.ts`
- Сравнение с `TELEGRAM_BOT_SECRET` из переменных окружения
- Возврат 401 при неправильном токене

**Задача 15: Защита от race condition при создании заказа** ✓
- Все операции обёрнуты в одну транзакцию SQL
- Используется встроенная функция `transaction()` из `lib/db.ts`

---

### 📊 Метрики

| Категория | Статус | Прогресс |
|-----------|--------|----------|
| 🔴 Критические | 7/18 выполнено | 39% |
| 🟠 Высокий приоритет | 0/24 | 0% |
| 🟡 Средний приоритет | 0/31 | 0% |
| ⚪ Низкий приоритет | 0/19 | 0% |
| **ИТОГО** | **7/142** | **4.9%** |

---

### 🔧 Технические улучшения

- **Безопасность**: Исправлены потенциальные SQL-инъекции в 5+ файлах
- **Валидация**: Добавлены белые списки полей для防止SQL injection
- **Rate Limiting**: Применён на критичные эндпоинты (платежи, роли, webhook)
- **Webhook**: Добавлена верификация токена для Telegram Bot API

---

### 🎯 Следующие шаги (11 оставшихся 🔴 задач)

1. CSRF защита (задача 8)
2. Применение миграций БД (задача 9)
3. Создание таблицы admin_logs (задача 10)
4. Инициализация super_admin (задача 11)
5. Реализация ролей courier и support (задача 12)
6. Обработка pre_checkout_query в боте (задача 13) — уже должна быть
7. Идемпотентность платежей (задача 14)
8. Удаление console.log (задача 16)
9. Валидация входных данных (Zod) (задача 17)
10. Добавление индексов БД (задача 18)

